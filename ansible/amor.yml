- hosts: simulation
  roles:
    - common
    - motors
    - magnet 
    - epics

- hosts: kafka_server
  tags:
    - kafkaServer
  vars:
    use_kafka: true
  roles:
    - { role: kafka, when: "{{ is_kafka_server }} == true" }


- hosts: generator_host
  tags:
    - neventGenerator
  roles:
    - generator
